{
  "version": "1.0",
  "name": "Fortune500_Governance_Policy",
  "policies": {
    "Research_Integrity": {
      "checkpoints": ["research_dossier_finalize"],
      "gates": {
        "citation_required": {
          "tool": "research_citations_analyze",
          "condition": "result.unsupportedClaims.length === 0",
          "failure_message": "Policy violation: 'citation required' enforcement failed. Dossier cannot be finalized with unsupported claims (G1)."
        }
      }
    },
    "Proposal_Compliance": {
      "checkpoints": ["proposal.package.export"],
      "gates": {
        "assumptions_exclusions_check": {
          "tool": "proposal.generate",
          "condition": "artifact.assumptions.length > 0 && artifact.exclusions.length > 0",
          "failure_message": "Policy violation: Proposal must explicitly include 'assumptions' and 'exclusions' (G1)."
        },
        "pii_security_check": {
          "tool": "security.scan",
          "condition": "domain.isPII && audit.secretsScanned",
          "failure_message": "Policy violation: PII detected, but security appendix/secrets scan not performed (G2, G3)."
        }
      }
    }
  }
}